# .env.local を作成し、以下の値を実際の値に置き換えること。
# .env.local はサーバー側のみで使用する秘密情報のため、絶対にGitにコミットしない(.gitignoreで除外済み)。

# Supabase (Project Settings > API から取得)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic Claude API
ANTHROPIC_API_KEY=

# 簡易パスコード認証(ログイン画面で照合する合言葉)
APP_PASSCODE=
